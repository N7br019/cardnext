import card from "../page.module.scss";

const page = () => {
  return (
    <main>
      <section id={card.cardContainer}>
        <div id={card.card}>

            <div id={card.headline}>pop</div>
            <div id={card.imagecon}>pop</div>
            <div id={card.content}>pop</div> 

        </div>
      </section>
    </main>
  );
};

export default page;
